let files = []; const drop = document.getElementById('drop'); drop.onclick = () => document.getElementById('fileInput').click(); drop.ondrop = e => { e.preventDefault(); files = [...e.dataTransfer.files]; document.getElementById('status').innerText = files.length + ' files loaded'; }; drop.ondragover = e => e.preventDefault(); document.getElementById('fileInput').onchange = e => { files = [...e.target.files]; }; async function upload() { const form = new FormData(); files.forEach(f => form.append('files', f)); document.getElementById('status').innerText = 'Processing...'; document.getElementById('bar').style.width = '30%'; const res = await fetch('/analyze', { method: 'POST', body: form }); document.getElementById('bar').style.width = '80%'; const blob = await res.blob(); const url = URL.createObjectURL(blob); const a = document.createElement('a'); a.href = url; a.download = 'analysis.xlsx'; a.click(); document.getElementById('bar').style.width = '100%'; document.getElementById('status').innerText = 'Done'; }